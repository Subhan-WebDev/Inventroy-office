<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Assign Item</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

  <header class="bg-blue-500 text-white py-4">
    <div class="container mx-auto text-center">
      <h1 class="text-3xl font-semibold">Assign Item</h1>
    </div>
  </header>

  <main class="container mx-auto p-6">

    <section class="bg-white shadow-lg rounded-lg p-6 max-w-4xl mx-auto">
      <h2 class="text-xl font-semibold text-center mb-6">Assign Item Details</h2>
      <form action="/form/<%= item._id %>" method="POST" class="space-y-6">

        <div class="flex flex-col">
          <label for="name" class="font-medium text-gray-700">Name:</label>
          <input type="text" name="name" id="name" value="<%= item.name %>" class="border border-gray-300 rounded-lg p-2 mt-1 focus:outline-none focus:ring-2 focus:ring-blue-500" required />
        </div>

        <div class="flex flex-col">
          <label for="team" class="font-medium text-gray-700">Team Status:</label>
          <select name="team" required id="team" class="border border-gray-300 rounded-lg p-2 mt-1 focus:outline-none focus:ring-2 focus:ring-blue-500">
            <option value="" disabled selected>Select</option>
            <option value="Cr" <%= item.status === 'Cr' ? 'selected' : '' %>>CR</option>
            <option value="Dev" <%= item.status === 'Dev' ? 'selected' : '' %>>Dev</option>
            <option value="Rs" <%= item.status === 'Rs' ? 'selected' : '' %>>RS</option>
          </select>
        </div>

        <div class="flex flex-col">
          <label for="type" class="font-medium text-gray-700">Type:</label>
          <input type="text" name="type" id="type" value="<%= item.type %>" class="border border-gray-300 rounded-lg p-2 mt-1 focus:outline-none focus:ring-2 focus:ring-blue-500" required />
        </div>

        <div class="flex flex-col">
          <label for="status" class="font-medium text-gray-700">Status:</label>
          <select name="status" id="status" class="border border-gray-300 rounded-lg p-2 mt-1 focus:outline-none focus:ring-2 focus:ring-blue-500">
            <option value="assigned" <%= item.status === 'assigned' ? 'selected' : '' %>>Assigned</option>
            <option value="faulty" <%= item.status === 'faulty' ? 'selected' : '' %>>Faulty</option>
            <option value="unassigned" <%= item.status === 'unassigned' ? 'selected' : '' %>>Unassign</option>
          </select>
        </div>

        <div class="text-center">
          <button type="submit" class="bg-blue-500 text-white py-3 px-6 rounded-lg shadow-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">Save</button>
        </div>

      </form>
    </section>

  </main>



</body>
</html>
